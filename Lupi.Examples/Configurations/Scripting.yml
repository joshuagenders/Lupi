---
scripting: 
  scripts:
    homepage:
      script: |
        var stopwatch = new Stopwatch();
        stopwatch.Start();
        var randomNumber = g.helpers.Rand()
        await g.httpClient.GetAsync("https://<website>.com/", ct);
        stopwatch.Stop();
        return (stopwatch, "success");
  scenario:
    - homepage

concurrency:
  threads: 10
  rampUp: 2m
  rampDown: 20s

throughput:
  thinkTime: 500ms

baseConfig: BaseScripts.yml