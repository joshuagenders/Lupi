concurrency:
    openWorkload: true
    minThreads: 3
    maxThreads: 180
throughput:
    tps: 300
    rampUp: 2m
    holdFor: 10m
    rampDown: 1m
listeners:
    statsd:
        bucket: openworkload
baseConfig: BaseConfig.yml
