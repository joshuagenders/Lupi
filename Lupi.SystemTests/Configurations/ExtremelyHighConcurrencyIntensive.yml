test:
    testMethod: Intensive
    singleTestClassInstance: false

listeners:
    statsd:
        prefix: ${STATSD_PREFIX}.extremelyhighconcurrencyintensive

concurrency:
    threads: 1500
    holdFor: 45s

baseConfig: base-config.yml
