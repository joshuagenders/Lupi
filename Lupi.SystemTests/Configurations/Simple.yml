test:
    testMethod: Increment

listeners:
    statsd:
        bucket: simple

concurrency:
    threads: 1
    holdFor: 1m

throughput:
    tps: 20
    rampUp: 20s
    holdFor: 20s
    rampDown: 20s
    thinkTime: 500ms

baseConfig: ../base-config.yml
