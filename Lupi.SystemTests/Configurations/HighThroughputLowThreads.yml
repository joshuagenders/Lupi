test:
    testMethod: TimedGetTimespan

listeners:
    statsd:
        bucket: highthroughputlowthreads

concurrency:
    threads: 10
    holdFor: 30s

throughput:
    tps: 400
    holdFor: 30s

baseConfig: base-config.yml
